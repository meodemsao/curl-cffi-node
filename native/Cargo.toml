[package]
name = "curl-cffi-node-native"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
napi = { version = "3.8.4", default-features = false, features = ["napi6"] }
napi-derive = "3.5.3"

[build-dependencies]
napi-build = "2.3.1"
